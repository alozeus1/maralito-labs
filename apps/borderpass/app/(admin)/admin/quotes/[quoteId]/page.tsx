// Protected by (admin) guard. Phase 3 placeholder — quote detail + status history + approvals.
export default function AdminQuoteDetailPage() {
  return (
    <main className="p-6">
      <h1 className="font-heading text-2xl">Quote detail</h1>
      <p className="text-on-surface-variant mt-2">
        Line items, finance approval, status history (Phase 3 foundation).
      </p>
    </main>
  );
}
