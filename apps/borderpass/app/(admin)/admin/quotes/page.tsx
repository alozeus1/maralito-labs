// Protected by (admin) guard (staff only). Phase 3 placeholder — quote queue.
export default function AdminQuotesPage() {
  return (
    <main className="p-6">
      <h1 className="font-heading text-2xl">Quotes</h1>
      <p className="text-on-surface-variant mt-2">Draft, approve, send (Phase 3 foundation).</p>
    </main>
  );
}
