# BorderPass / Maralito — Documentation Index

All planning + architecture documents, relocated into a clear taxonomy during Phase 0.

| Folder | Contents |
|--------|----------|
| `product/` | BorderPass PRD / product architecture (01–20), MVP scope, edge cases, roadmap |
| `architecture/` | Technical Architecture, Data/API/Event/Access **contracts**, Maralito Platform architecture |
| `ai/` | AI Agent + LangGraph architecture blueprint |
| `automation/` | Maralito Automation Platform architecture |
| `design/` | Design-to-Frontend Handoff + approved Stitch board (`design-reference/`) |
| `operations/` | Operations index (canonical ops specs live in `product/`) |
| `security/` | Security index (canonical security specs live in `architecture/`) |
| `decisions/` | Delivery plans, readiness reviews, Master Build Package, and **ADRs** (`adr/`) |
| `phase-0/` | Phase 0 build docs (README, architecture notes, spike results, gaps, Phase 1 checklist) |

> **Relocation note (Phase 0):** docs were moved from `borderpass/*` and `maralito-platform/*`
> into this taxonomy. Same-folder relative links are preserved; a few **cross-folder** links
> (e.g. `../contracts/...`) may need updating — tracked in `phase-0/known-gaps.md`.
