# Phase 7 — Row 11 OTP Smoke — EVIDENCE (PASS)

- **Timestamp (UTC):** 20260708T045425Z
- **Conclusion:** ✅ **PASS** — full OTP → session → provisioning → relogin proven on the LIVE project.
- **Mode:** development-only · synthetic-only · no email · no live payments · no codes/tokens/keys logged.

## Method

`packages/db/scripts/row11-otp-smoke.ts` — mints a real one-time code via the Supabase **admin API**,
verifies it with the **anon key** to a real session, then runs the app's actual `provisionUserCore` twice
through `withPrivilegedDbAccess` (the same privileged DB path the `/auth/callback` uses), asserts idempotency,
reloads with a fresh OTP, and cleans up the synthetic rows + auth user. Ran against the live `borderpass-dev-gate`
project (ref `rupqejwzmwfspvbmkmai`) with the **rotated** Supabase keys + the **new DB password**.

## Results (yes/no)

| Check | Result |
|---|---|
| OTP minted via Supabase admin API (no email, no rate limit) | ✅ PASS |
| OTP verified → real session created | ✅ PASS |
| Authenticated user id present | ✅ PASS |
| Provisioning idempotent — exactly 1 identity (after 2 runs) | ✅ PASS |
| Provisioning idempotent — exactly 1 customer role | ✅ PASS |
| Provisioning idempotent — exactly 1 baseline profile | ✅ PASS |
| Relogin (fresh OTP) → session | ✅ PASS |
| Relogin — no duplicate provisioning (still 1 role) | ✅ PASS |
| Cleanup (synthetic rows + auth user removed) | ✅ PASS |
| **Overall** | **ROW 11 SMOKE: ALL PASS** |

## Notes

- This exercised the real Supabase Auth token flow + the app's real provisioning code against the live schema
  (unique constraints + `onConflictDoNothing` → idempotent). Custom SMTP (Resend) is configured, so real
  magic-link email delivery is also available for on-device tester login; the programmatic smoke intentionally
  bypasses email to test the auth+provisioning core deterministically.
- Row 11 → ✅ PASS in `gate-ledger.md`.
