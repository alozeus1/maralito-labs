{
  "name": "@maralito/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:seed": "tsx src/seed/dev-seed.ts",
    "test:rls": "vitest run tests/rls.isolation.test.ts",
    "gate:rls": "tsx scripts/live-rls-gate.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.33.0",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "drizzle-kit": "^0.24.2",
    "tsx": "^4.19.1",
    "vitest": "^2.1.2",
    "@maralito/config": "workspace:*",
    "@electric-sql/pglite": "^0.5.3"
  }
}
